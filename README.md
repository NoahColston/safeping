# safeping
SafePing is a mobile application that provides structured, customizable safety check-ins with automatic escalation. Users schedule recurring check-ins. If a user fails to respond within a configured window, designated contacts are alerted.
